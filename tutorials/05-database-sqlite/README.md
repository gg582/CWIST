# Tutorial 05: Embedded SQLite Database

SQLite 메모리 DB 생성 및 테이블 쿼리 실행, cJSON 변환 예제입니다.

## 빌드 및 실행

```bash
mkdir build && cd build
cmake ..
make
./tut05
```

`curl`로 테스트:
```bash
curl http://127.0.0.1:8084/db
```
